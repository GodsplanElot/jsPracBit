/* Single letter form name generator*/

/* let myName = "Godsplan";

console.log(myName.charAt(Math.floor(Math.random() * myName.length))) */

/* Stops here */


if (condition) {
    // run some code
} else {
    //run some other code
}
